production:
#  read_only: true # default: false
#  github_secure: "think_of_secure_token" # default: nil
  url_host: "<%= "#{node[:bigtuna][:ip]}:#{node[:bigtuna][:production_port]}" %>"
  ajax_reload: "always" # "building" or "always"

secure_production:
  read_only: true # default: false
#  github_secure: "think_of_secure_token" # default: nil
  url_host: "<%= "#{node[:bigtuna][:ip]}:#{node[:bigtuna][:secure_production_port]}" %>"
  ajax_reload: "always" # "building" or "always"
